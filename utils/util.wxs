var truncate = function(str, num){
  if (null == str) {
    return str
  }
  return num > str.length ? str : num >= 3 ? str.slice(0, num - 3) + '...' : str.slice(0, num) + '...'
}
module.exports = {
  truncate: truncate
}
